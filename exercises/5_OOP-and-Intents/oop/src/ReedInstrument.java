/** * Created by Hoshiko on 4/30/15. */public abstract class ReedInstrument implements Instrument {    abstract boolean hasReed();    // puts a reed on the instrument    abstract void place_reed();    //removes the reed from the instrument    abstract void remove_reed();    //Why an abstract class implementing an interface can miss    // the declaration/implementation of one of the interface's methods?    // Because if a class is abstract, then by definition you are required to create    // subclasses of it to instantiate. The subclasses will be required    // (by the compiler) to implement any interface methods that the abstract class left out.}